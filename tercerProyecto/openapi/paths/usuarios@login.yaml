get:
  tags:
    - Usuarios
  summary: Loguea al usuario
  parameters:
    - name: username
      in: query
      description: Usuario
      required: true
      schema:
        type: string
    - name: password
      in: query
      description: Contraseña
      required: true
      schema:
        type: string
        format: password
  responses:
    '200':
      description: Exito
