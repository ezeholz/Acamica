put:
  tags:
    - Pedidos
  summary: Modificar Status de Pedido
  parameters:
    - name: id
      in: path
      description: ID del pedido
      required: true
      schema:
        type: integer
    - name: status
      in: query
      description: Status del pedido
      required: true
      schema:
        type: string
  responses:
    '200':
      description: Exito
